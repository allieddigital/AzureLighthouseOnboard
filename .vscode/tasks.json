{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Bicep",
            "type": "shell",
            "command": "bicep build ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
